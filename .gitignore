# Files
*.egg-info
*.pyc
*.tmp_profiler_run
.vscode
html
._.DS_Store
*_updated.mseed

# Folders
.ipynb_checkpoints
__pycache__
dist
build
.tox
scratch
env
venv
.venv
__MACOSX

# Tooling caches
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov

# Raw data must never enter Git (large, immutable, externally sourced)
datasets/**/raw/
datasets_global/**/raw/

# Derived artifacts (rebuildable from raw + config)
artifacts/curves/
artifacts/datasets/
artifacts/runs/
outputs/

# Local waveform fixtures and scratch work (large or machine-specific)
data_test/
tmp/

# Checksum cache (rebuildable, machine-specific mtimes)
artifacts/cache/
